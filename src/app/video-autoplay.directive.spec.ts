import { VideoAutoplayDirective } from './video-autoplay.directive';

describe('VideoAutoplayDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoAutoplayDirective();
    expect(directive).toBeTruthy();
  });
});
